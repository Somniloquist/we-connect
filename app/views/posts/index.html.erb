<% provide(:title, "Home") %>
<div class="container">
  <h1>Latest Posts</h1>
  <%= render "form" %>
  <% @posts.each do |post| %>
    <div class="post-card">
     <%= render post %>
    </div>
  <% end %>
  <%= will_paginate @posts %>
</div>