
    <%= form_with model: @friendship, id: "friend-request-form" do |f| %>
      <div><%= hidden_field_tag :friendship_id, @friendship.id %></div>
      <%= f.submit "Accept", class: "btn btn-primary" %>
    <% end %> 